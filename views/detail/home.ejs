<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Inter:wght@100;400;500;900&display=swap");
      :root {
        --primary: #191919;
        --secondary: #ff6b1a;
        --ternary: #310273;
        --background: #f2cdac;
        --gray: #e1eeff7f;
        --white: #fff;
      }

      body {
        color: var(--font-color);
        font-family: "Inter", sans-serif;
        font-size: 1.2em;
        line-height: 1.6;
        background: var(--background);
        overflow-x: hidden;
        min-height: 100vh;
        min-width: 100vw;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 20px;
      }

      .card {
        width: 300px;
        height: 300px;
        border-radius: 50%;
        position: relative;
        background-color: var(--secondary);
        cursor: pointer;
      }

      .person {
        width: 300px;
        height: 360px;
        display: flex;
        justify-content: center;
        align-items: center;
      }
      .person > img {
        border-radius: 50%;
        width: 220px;
        height: 220px;
        top: 15px;
        position: relative;
      }
      .person > img:hover {
        animation: animate 1s forwards;
      }

      @keyframes animate {
        100% {
          position: relative;
          transform: scale(1.6);
          top: -55px;
          border-radius: 60%;
        }
      }
      .title {
        font-weight: 500;
        initial-letter: 1.5;
      }
      @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400;500&display=swap");
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", sans-serif;
}
nav {
position: absolute;
top: 20px;
  background: #fff;
  border-radius: 9px;
  padding: 10px;
  box-shadow: 0 5px 20px rgba(0, 0, 0, 0.4);
}
nav ul li {
  display: inline-block;
  list-style: none;
  font-size: 1rem;
  padding: 0 10px;
  margin: 0 20px;
  cursor: pointer;
  position: relative;
}
nav ul li:after {
  content: "";
  width: 0;
  height: 3px;
  background: #ffcc00;
  position: absolute;
  left: 0;
  bottom: -10px;
  transition: 0.5s;
}
nav ul li:hover::after {
  width: 100%;
}

    </style>
  </head>
  <body>
        <nav>
          <ul>
            <a href="/"><li>Home</li></a>
            <a href="/changepassword?username=<%= result[0].username %>"><li>Change Password</li></a>
            <a href="/show?username=<%= result[0].username %>"><li>Profile</li></a>
            <a onclick="logout()"  href="/logout"><li>Logout</li></a>
          </ul>
        </nav>
    <div style="display: flex; align-items: center; gap: 150px;">      

        <div class="card">
            <div class="person">
              <img
                src="https://cdn-icons-png.flaticon.com/512/2922/2922653.png"
                alt=""
              />
            </div>
          </div>

        <ul>
        <li style="text-transform: capitalize" class="title">
          Name: <%= result[0].fname %>
        </li>
        <li class="title">
          Username: <%= result[0].username %>
        </li>
        <li style="text-transform: capitalize" class="title">
          Gender: <%= result[0].gender %>
        </li>
        <li style="text-transform: capitalize" class="title">
          Phone number: <%= result[0].phno %>
        </li>
      </ul></div>
      <script>
        const logout = () =>{
                localStorage.clear()
            }
    </script>
  </body>
</html>
